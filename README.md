# Simple-NodeJS-HTTP-Server-Example
Simple NodeJS Http Server Example

Super simple sample on getting a NodeJS server to run, respond and file write command.

First: Make sure node is installed. 
node -v 
This was built with node v25.2.0

Run:
node index.js